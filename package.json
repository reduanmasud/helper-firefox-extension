{
  "name": "qa-tools",
  "version": "1.0.0",
  "description": "QA Tools - A powerful Firefox extension for test engineers to create, manage, and execute JavaScript scripts with advanced element inspection capabilities",
  "main": "background/background.js",
  "scripts": {
    "build": "node build.js && web-ext build --config=web-ext-config.cjs",
    "build:bundles": "node build.js",
    "lint": "web-ext lint --config=web-ext-config.cjs",
    "start": "npm run build:bundles && web-ext run --config=web-ext-config.cjs",
    "dev": "npm run build:bundles && web-ext run --config=web-ext-config.cjs"
  },
  "keywords": [
    "firefox-extension",
    "test-automation",
    "javascript",
    "element-inspector",
    "script-manager",
    "xpath",
    "playwright",
    "qa-tools",
    "test-engineering"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.25.8",
    "web-ext": "^8.9.0"
  },
  "dependencies": {
    "@codemirror/lang-javascript": "^6.2.4",
    "codemirror": "^6.0.2",
    "dompurify": "^3.2.6",
    "marked": "^16.1.2"
  }
}
